<h1 class="text-center mt-4 mb-3">Alpha Bloggerss</h1>

<div class="flickr_pagination">
  <%= will_paginate @user, :container => false %>
</div>

<% @user.each do |user| %>
  <div class="container">
    <div class="row justify-content-md-center mt-4">
      <div class="col-8">
        <div class="card text-center shadow mb-5 bg-body-tertiary rounded">
          <div class="card-header fst-italic">
            <%= link_to user.username , user %>
          </div>
          <div class="card-body">
            <h5 class="card-title"><%= link_to gravatar_for(user,size: 200) , user %></h5>
            <p class="card-text"><%= pluralize(user.articles.count, "countes") %></p>
            <% if logged_in? %>
              <% if user == current_user %>
                <%= link_to "View Profile" , user_path(user) , class:"btn btn-outline-success"%>
                <%= link_to "Edit Profile" , edit_user_path(user) , class:"btn btn-outline-info"%>
              <% end %>
              <% if current_user.admin? %>
              <%= link_to "Delete Profile", user_path(user) , data:{turbo_method: "delete" , turbo_confirm: "Are you sure you want to delete the user account and all associated articles?"} , class:"btn btn-outline-danger"%>
              <% end %>
            <% end %>
          </div>
          <div class="card-footer text-body-secondary">
            <small>
              Joined <%= time_ago_in_words(user.created_at) %> ago
            </small>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<div class="flickr_pagination">
  <%= will_paginate @user, :container => false %>
</div>